<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Asignar Materia a Docente</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="w-full bg-gradient-to-b from-[#190019] to-[#dfb6b2] flex items-center justify-center min-h-screen p-4">
    <div class="w-full bg-gradient-to-b from-[#190019] to-[#dfb6b2] p-8 rounded-lg shadow-lg w-full max-w-2xl">
        <h1 class="text-3xl font-bold text-center text-[#ccd0fc] mb-6">Asignar Materia a Docente</h1>

        {{#successMessage}}
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{successMessage}}</span>
        </div>
        {{/successMessage}}

        {{#errorMessage}}
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{errorMessage}}</span>
        </div>
        {{/errorMessage}}

        <form action="/teacher/assign-materia" method="post" class="space-y-6">
            <div>
                <label class="block text-[#ccd0fc] text-sm font-medium mb-2">Docente:</label>
                <select name="teacher_id" required class="w-full px-4 py-3 border rounded-md bg-white text-black">
                    <option value="">Seleccione un docente</option>
                    {{#teachers}}
                    <option value="{{id}}">{{label}}</option>
                    {{/teachers}}
                </select>
            </div>

            <div>
                <label class="block text-[#ccd0fc] text-sm font-medium mb-2">Materia:</label>
                <select name="materia_id" required class="w-full px-4 py-3 border rounded-md bg-white text-black">
                    <option value="">Seleccione una materia</option>
                    {{#materias}}
                    <option value="{{id}}">{{label}}</option>
                    {{/materias}}
                </select>
            </div>

            <button type="submit" class="w-full bg-yellow-500 text-white py-3 px-4 rounded-md hover:bg-yellow-700 transition duration-300">
                Asignar materia
            </button>

            <div class="mt-8 pt-6 border-t border-[#ccd0fc]/20 flex justify-center">
                <a href="/dashboard" class="flex items-center gap-2 text-[#ccd0fc] hover:text-white transition-colors duration-200">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                    </svg>
                    <span>Volver al Dashboard</span>
                </a>
            </div>
        </form>
    </div>
</body>
</html>
